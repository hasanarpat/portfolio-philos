import { AboutLayout } from "@/components/about-layout"

export const metadata = {
  title: "About — Who Builds This",
  description: "Background, principles, and contact information.",
}

export default function AboutPage() {
  return <AboutLayout />
}
